# TAKIBO UI — variables d'environnement frontend (préfixe VITE_ obligatoire).
# Ne jamais placer de secret ici : tout ce qui commence par VITE_ est embarqué
# dans le bundle et visible par le navigateur.

# URL de base du futur BFF TAKIBO UI (récit TAKIBO UI 02).
VITE_BFF_BASE_URL=http://localhost:8080
